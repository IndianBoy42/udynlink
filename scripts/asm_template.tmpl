    .syntax unified
    .arch armv7-m

    .text
    .thumb

{% for s, actname in sym_names.items() %}
    .thumb_func
    .align 1
    .section .text_nogc, "x"
    .globl {{s}}
    .type {{s}}, %function
    .extern {{actname}}
    .type {{actname}}, %function
{{s}}:
    push    {r9, lr}
    bl      {{actname}}
    pop     {r9, pc}

    .size   {{s}}, . - {{s}}
{% endfor %}

    .end
